# TrimsSilver — Project Status & Handoff

Written 2026-08-17, updated same day after implementing the ingest endpoints, then the Discord OAuth client flow, then confirming the whole thing works live in production. Updated again 2026-08-18 after building and shipping the Market Prices page. Updated again 2026-08-27 to cover the Crafting Calculator (shipped 2026-08-24) and a new client issue, then again the same day after shipping the Flipper. Updated again 2026-08-29 after shipping the Journals Calculator. Updated again 2026-08-31 after shipping Craft Finder. Read this first in a new session — it's the durable record; a fresh Claude Code session on a different machine has no memory of this conversation.

## What TrimsSilver is

A fork of JPCodeCraft's "AFM Data Client" (`AlbionDataAvalonia`, an Albion Online market-data sniffer, originally for Albion Free Market) being turned into an independent product with its own backend and its own auth, no longer affiliated with AFM. The client fork keeps a required attribution link back to the original repo in its README (per the fork's LICENSE terms).

- **Client repo:** https://github.com/Rabhynoide/TrimsSilver-Client (branch `master`)
- **Server repo:** https://github.com/Rabhynoide/TrimsSilver-Server (branch `main`) — this repo
- **Backend URL:** https://trimssilver.trimards-island.org — **live**, deployed as a Docker Compose stack via Portainer on the user's own server. Verified reachable with the Discord sign-in button rendering.
- **Auth:** Discord OAuth (via Auth.js), not the old Google/Firebase flow AFM used
- Local clones live side by side on the original dev machine under `...\GitHub\Albion\TrimsSilver-Client` and `...\GitHub\Albion\TrimsSilver-Server`.

## Stack

- **Server (this repo):** Next.js 16 (TypeScript, App Router, Tailwind) + Prisma 7 + PostgreSQL + Auth.js v5 (Discord provider, database sessions via `@auth/prisma-adapter`). Prisma 7 requires an explicit driver adapter (`@prisma/adapter-pg` + `pg`) — see `src/lib/prisma.ts`; there's no more implicit native engine like Prisma 5/6. Dockerized (`Dockerfile` + `docker-compose.yml`, app + Postgres, migrations run automatically on container boot).
- **Client:** unchanged .NET/Avalonia desktop app — see `TrimsSilver-Client/AGENTS.md` for its own build instructions.

## Status as of 2026-08-17 (end of day)

### Done
- Server scaffolded; Discord sign-in works end-to-end (user tested it live in a browser).
- Client fully rebranded from AFM to TrimsSilver at the code level: class/identifier renames (`AFMUploader`→`TrimsSilverUploader`, `Afm*` settings fields→`TrimsSilver*`, etc.), app identity (AppData folder, Inno Setup installer, macOS bundle id `org.trimardsisland.trimssilver`, Linux install scripts, self-update URLs now point at this GitHub org). Commit `630d31a` on `TrimsSilver-Client`, build verified with 0 errors. Client issue #1 closed.
- Removed the client's old behavior of downloading `AppSettings` from AFM's CDN at runtime (`SettingsManager.cs`) — it would have silently broken with the renamed settings fields, and no longer made sense for an independent fork anyway.
- **Server deployed live** via Docker/Portainer stack on the user's own server (see Deployment section below for the fixes this took).
- **Prisma schema + ingest endpoints for all 7 private upload types implemented, tested end-to-end against a local dev DB, and deployed** (issue #1 scope). See "Private data model" below.
- **Client issue #3 done: `AuthService.cs` now does Discord OAuth against TrimsSilver-Server instead of Google/Firebase.** Client build verified with 0 new errors/warnings.
- **Client issue #2 done: `TrimsSilverIngestApiBase` points at the live server.**
- **✅ Full end-to-end chain confirmed working live in production**, with a real Discord account and the real game running — not just curl tests. See "Confirmed working end-to-end" below.

### Deployment (Docker/Portainer) — fixes applied this session
Two things broke the first Portainer stack deploy, both now fixed and committed:
1. **`env_file: - .env` doesn't work with Portainer.** Portainer's stack "Environment variables" UI doesn't necessarily materialize a `.env` file next to `docker-compose.yml` on the host, so `env_file: - .env` failed with `.env not found`. Fixed by switching `docker-compose.yml`'s `app` service to `environment: { AUTH_SECRET: ${AUTH_SECRET:?...}, ... }`-style interpolation instead — Portainer's env var UI feeds `${VAR}` substitution directly, file or not. **You must set `AUTH_SECRET`, `AUTH_URL` (the real public URL, not localhost), `AUTH_DISCORD_ID`, `AUTH_DISCORD_SECRET` in Portainer's stack env var UI** — `DATABASE_URL` is hardcoded in the compose file and doesn't need to be set there.
2. **`Dockerfile` copied a `public/` folder that didn't exist** (Next.js App Router doesn't need one — favicon lives in `src/app/`), so the Docker build failed on `COPY --from=builder /app/public ./public`. Fixed by adding `public/.gitkeep` so the folder is tracked in git and exists in the build context.

### Private data model (issue #1)
`prisma/schema.prisma` now has, beyond the Auth.js tables: `MarketOrder`, `PlayerCount`, `AchievementSnapshot`+`AchievementEntry`, `GlobalMultiplier`, `FestivitySnapshot`+`FestivityEvent`, `ItemEstimatedMarketValue`, `PrivateOrderShare`, plus an `ApiToken` model for API auth (see below). Migration `20260817120000_add_private_ingest_models` adds them; `Dockerfile`'s `npx prisma migrate deploy` on container boot applies it automatically on next Portainer redeploy — no manual DB step needed.

Design notes worth knowing before extending this:
- Every private row is linked to the authenticated uploader via a relation to `User` — **client-supplied ids in the payload (e.g. `uploaderId`) are always ignored**, the uploader is resolved server-side from the bearer token.
- `MarketOrder` and `ItemEstimatedMarketValue` are upsert-as-current-state tables (unique on `(serverId, orderId)` / `(serverId, itemUniqueName, quality, day)`), not append logs.
- `AchievementSnapshot`/`FestivitySnapshot` replace their child rows wholesale on every upload (delete + recreate in a transaction) rather than diffing.
- `PlayerCount` is the only true append-only time series.
- `PrivateOrderShare`: **identifier resolution isn't implemented** — every submitted value is stored and returned as `resolved: false, type: "unresolved"`. No design exists yet for matching a shared identifier (e.g. a Discord tag) to a real TrimsSilver account; that's a real gap, not an oversight to silently work around.
- `serverId` is a plain `Int` (1=Americas, 2=Asia, 3=Europe, hardcoded client-side in `AlbionServers.cs`) — no reference table needed.

### API auth: bearer tokens, not the Auth.js session cookie
The desktop client has no cookie jar, so it can't use Auth.js's database session cookie directly. Added:
- `ApiToken` Prisma model (hashed token storage, never the raw value; no expiry — tokens are long-lived personal-access-token style, revoked by deleting the row, not rotated).
- `src/lib/api-auth.ts`: `mintApiToken(userId, label)` (shared minting helper) and `requireApiUser(request)` (validates `Authorization: Bearer <token>` on every ingest route).
- `POST /api/tokens` — mints a token for the currently signed-in browser session directly (for manual use / a future dashboard). `GET /api/tokens` lists a user's tokens (metadata only).
- **`GET /cli-auth?redirect_uri=<loopback-url>`** — what the desktop client actually opens in the system browser. Signs the user into Discord if needed (via a click-through button — `next-auth`'s `signIn()` mutates cookies, which Next.js only allows inside a Server Action, not a bare page render, so it can't happen automatically on page load), then a second click-through ("Autoriser") mints a token via a Server Action and redirects to `redirect_uri` with `?token=...` attached. **`redirect_uri` is validated server-side to be `http://localhost`, `http://127.0.0.1`, or `http://[::1]`, rejecting everything else** — this hands out a live credential, so allowing arbitrary redirect targets would be an open-redirect-to-credential-leak vulnerability.
- **`GET /api/me`** — validates a bearer token and returns `{id, name, email, image}`, or 401. Since `ApiToken` never expires, this fully replaces the old Firebase refresh-token exchange: a token is either still accepted or it isn't, nothing to refresh.

This is no longer a stopgap — it's the actual, complete, tested flow. Only gap: no UI to revoke/list a lost device's token yet (would need a dashboard page calling `GET/DELETE` on `ApiToken` rows — not built).

### Client auth flow (`AuthService.cs`, client issue #3)
`SignInAsync()` now: opens the browser to `{TrimsSilverAuthUrl}?redirect_uri={TrimsSilverAuthRedirectUri}` (a local `HttpListener`, same pattern as before) → user signs into Discord + clicks "Autoriser" on `/cli-auth` → listener catches `?token=...` → client calls `GET {TrimsSilverIngestApiBase}/me` with that bearer token to fetch profile info → stores the token in the existing local SQLite `UserAuth` table (repurposed: the `RefreshToken` column now holds the non-expiring TrimsSilver token, not a Firebase refresh token — no EF migration needed, same column).

Because tokens don't expire, the whole refresh-scheduling machinery is gone: `EnsureValidTokenAsync()` is now a synchronous local presence check (no network call, safe to call before every upload), and `TryRecoverFromUnauthorizedAsync()`/`ForceTokenRefreshAsync()` both just re-validate against `GET /api/me` — log the user out only on a confirmed 401, leave the session alone on a transient network error.

`FirebaseAuthResponse` (the model) **keeps its name and shape** on purpose — every consumer (`MainViewModel`, `SettingsViewModel`, `LegendaryViewModel`, `PortfolioUploadService`, `LegendarySaleService`, `ItemEstimatedMarketValueBackendLoader`, `TrimsSilverUploader`) binds to `CurrentFirebaseUser`/`FirebaseUserChanged`/`FirebaseUserId`/`.IdToken`/`.LocalId` and none of that changed, so **none of those 8 files needed touching**. Renaming `FirebaseAuthResponse` itself is deferred alongside the other leftover AFM/Firebase-branded UI strings already noted below.

Settings changes in `AppSettings.cs`/`DefaultAppSettings.json`: `TrimsSilverAuthClientId` (Discord client id) is **gone** — the client never talks to Discord directly anymore, only to our own server. `TrimsSilverAuthApiUrl` is replaced by `TrimsSilverAuthUrl`, already set to the live `https://trimssilver.trimards-island.org/cli-auth`. `TrimsSilverAuthRedirectUri` is unchanged (`http://localhost:5000/`). `TrimsSilverIngestApiBase` is now `https://trimssilver.trimards-island.org/api/` — **trailing slash is load-bearing**, not cosmetic: `TrimsSilverUploader.cs` combines it with relative paths via `new Uri(base, relative)`, and without the trailing slash `Uri` (like any RFC 3986 resolver) drops the base's last path segment, silently sending every request to the domain root instead of `/api/...`. Caught and fixed in commit `eadc1ee` before it shipped.

### Confirmed working end-to-end (2026-08-17, live production)
Ran the client from source (`dotnet run --project AlbionDataAvalonia.Desktop`) to show how to test it, and the user signed in for real with their own Discord account while playing Albion Online. Server logs and client logs both confirm the full chain worked, live, against production — not a curl simulation:

```
21:51:00 Browser opened for Discord sign-in.
21:51:02 Received token from the auth redirect.
21:51:02 User signed in: re***************fr
21:51:27 Successfully sent 48 item estimated market values to AFM EMV endpoint.
21:51:37 Successfully sent global multiplier 1.156 for server 3 to AFM global multiplier endpoint.
21:51:38 Successfully sent 457 achievements for character Rabhynoide on server 3 to AFM achievements endpoint.
```

That's `/cli-auth` (Discord sign-in + consent + token mint) and three different ingest routes (`itemEstimatedMarketValues`, `be/globalMultiplier`, `be/achievements`) all round-tripping real data into production Postgres, driven entirely by the app's normal live-gameplay upload paths — no manual testing scaffolding involved. `flipperOrders`, `playercount`, `be/festivities`, and `privateOrderShares` weren't separately confirmed live this way (no matching gameplay event happened during the session) but share the same auth path and were already curl-verified against a local dev DB.

### Ingest endpoints (exact same relative paths as the old AFM ones on purpose)
So that client issue #2 only needs a settings change (`TrimsSilverIngestApiBase` → `https://trimssilver.trimards-island.org/api`), not a client code change:

| Route | Method | Old AFM path |
|---|---|---|
| `/api/flipperOrders` | POST | `flipperOrders` |
| `/api/playercount` | POST | `playercount` |
| `/api/be/achievements` | POST | `be/achievements` |
| `/api/be/globalMultiplier` | POST | `be/globalMultiplier` |
| `/api/be/festivities` | POST | `be/festivities` |
| `/api/itemEstimatedMarketValues` | POST | `itemEstimatedMarketValues` |
| `/api/privateOrderShares` | GET, PUT | `privateOrderShares` |

All tested end-to-end against a local dev DB with curl (auth rejection, successful writes, and upsert idempotency all verified working).

### Deliberately not done yet (by design, not forgotten)
- README screenshots/full content pass and new icon/logo artwork — design work, not code, out of scope for the rebrand pass.

### Client UI/log text rebranded — and a real bug found along the way (2026-08-18)
Went through every remaining "AFM" string in the client (`TrimsSilverUploader.cs` and everything it feeds, the app's own self-name in log lines and Settings/Dashboard text, the backup/restore instructions) and renamed the ones that now genuinely target TrimsSilver-Server. Deliberately left "AFM" wording alone in Portfolio, the Legendary item marketplace, and `OpenAFMWebsite()`/the AFM Discord link — those still call the real `api.albionfreemarket.com`, so the text was accurate, not stale. Commit `bc07800` on `TrimsSilver-Client`.

Found and fixed a real correctness bug while at it, unrelated to branding: the backup-restore instructions (`SettingsView.axaml`, `README.md`) referenced a database filename of `afmdataclient.db`, but `AppData.cs` has written `trimssilver.db` since the original rebrand — anyone following those steps literally would look for a file that no longer exists.

**Bigger finding**: verifying which backend each "AFM" string actually pointed at surfaced a real regression from the auth rewrite. `PortfolioUploadService`, `LegendarySaleService`, and `ItemEstimatedMarketValueBackendLoader` all send `authService.CurrentFirebaseUser.IdToken` as a Bearer token to the *real* AFM backend (`TrimsSilverBackendApiBase`/`TrimsSilverTopItemsApiBase`, still `api.albionfreemarket.com`). Since the Discord/TrimsSilver auth rewrite, that token is a TrimsSilver-only opaque token AFM has never issued and can't validate — those three features would 401 on every real request. **User decision: disable them for now** rather than leave them silently broken. Commit `fbd86a3` adds a single `FeatureFlags.AfmIntegrationEnabled = false` switch (in `AlbionDataAvalonia/Settings/FeatureFlags.cs`) that:
- Short-circuits Portfolio uploads before any network call (updated the "sign in to AFM" messaging, since being signed in wouldn't help anymore).
- Disables the Legendary view's "List for sale"/"Update price"/"Relist" actions with a clear status message — the item **tracking** table itself (locally captured, not AFM-dependent) is untouched and still fully functional.
- Disables the "Add to Portfolio" buttons in Trades/Gathering directly instead of letting them fail after a click.
- Stops `ItemEstimatedMarketValueBackendLoader` from queuing AFM lookups at all.

**Still undecided, needs a real product decision, not a code fix**: whether to (a) leave these retired permanently as AFM-branded premium features that don't fit an independent TrimsSilver, (b) reintroduce a second, parallel Google/Firebase auth flow just for these three features alongside the new Discord flow, or (c) something else. Flip `FeatureFlags.AfmIntegrationEnabled` back to `true` only once whichever auth path they'll actually use again is real.

### Key architecture decision (2026-08-17)
The client's **public AODP channel stays exactly as-is** — `MarketOrder`, `GoldPriceUpload`, `MarketHistoriesUpload`, `BanditEventUpload` keep uploading straight to `pow.*.albion-online-data.com` like today. No client changes needed for this, and TrimsSilver-Server does **not** need to ingest/store this data. Instead, TrimsSilver-Server will act as a **reader** of AODP's public REST API (`https://<region>.albion-online-data.com/api/v2/stats/...`, no auth needed) to fetch current/historical prices — both what we contribute and what everyone else contributes. This reader/cache service is not built yet (see Next steps).

## GitHub issues (source of truth for granular tracking)

Server (`Rabhynoide/TrimsSilver-Server`):
- #1 — Endpoint d'ingestion des données de marché — **closed**. Scope-revision comment posted (7 private types only, not public AODP data).
- #2 — Dashboard utilisateur — open. Comment posted noting its prerequisites (Discord auth, market ingestion) are done.
- #3 — Optimiser l'image Docker — open, unchanged (currently ships full `node_modules` incl. dev deps for reliability; revisit once there's more to containerize).
- #4 — Résolution d'identifiants pour `PrivateOrderShares` — new, open. Split out of the "Private data model" gap noted above.
- #5 — Page de gestion des tokens API (lister/révoquer) — new, open. Concrete first slice of #2, split out into its own issue since it's independently actionable.
- #6 — Service de lecture des données publiques AODP — **closed** (2026-08-29). Both halves done now: the live-proxy read (Market Prices' `GET /api/market/prices`) and the caching/storage half (`src/lib/priceCacheSync.ts`, Farming + Journals' item universe). See "Market-price cache" above.
- #7 — Market Prices : vue détaillée d'un item + historique de prix (graphique) — **closed**. Click any price cell to chart it; see "Market Prices page" below.
- #8 — Farming Calculator : territoires Outlands + élevage Beast/montures rares — new, open. Deliberately cut from the V1 scope; see "Farming & Breeding Calculator" below.
- #9 — Farming Calculator : lignes séparées par choix de produit animal (ex. Oie viande vs œufs) — new, open. Deliberately deferred, noted by the user; see "Follow-up fixes and refinements" below.

Client (`Rabhynoide/TrimsSilver-Client`):
- #1 — Rebranding AFM → TrimsSilver — **closed**, done
- #2 — Pointer le client vers TrimsSilver-Server — **closed**. Comment posted covering the trailing-slash `Uri` gotcha and the PoW question (not kept — Discord auth replaces it for the private channel; public AODP's PoW is untouched).
- #3 — Remplacer l'auth Google/Firebase par Discord OAuth dans `AuthService.cs` — **closed**. Comment posted covering the new flow and the `ItemEstimatedMarketValueBackendLoader`/Portfolio/Legendary auth breakage.
- #4 — Reset du versioning client à 0.x — open. Comment posted noting its prerequisite (rebranding, #1) is done.
- #5 — Portfolio, marketplace Legendary et lookups EMV AFM cassés (401) — new, open. Full writeup of the "Client UI/log text rebranded" finding above; this is where the actual product decision should get tracked/discussed, not just here.
- #6 — Auto-update silencieux pour Linux/macOS (parité avec Windows) — open, not yet covered elsewhere in this doc. Linux/macOS currently require re-running the installer manually to update (see the Linux install/uninstall instructions in `TrimsSilver-Client/README.md`); Windows already has silent auto-updates.
- #7 — "Private Flips Mode" ne persiste pas et se désactive à chaque déconnexion — new, open. See "Client bug found while testing live" under "Flipper" above.
- #10 — Crafting Calculator: Resource Return Rate non auto-calculé — new, open. See "Crafting Calculator" above.
- #11 — Crafting Calculator: mode de prix « My EMV » manquant (qualité 1-5) — new, open. See "Crafting Calculator" above.
- #12 — Crafting Calculator: vérification live pas encore faite — new, open. See "Crafting Calculator" above.
- #13 — Flipper: Public Flips (agrégation cross-utilisateurs) — new, open. See "Flipper" above.
- #14 — Flipper: tracking "consommé" + statistiques horaires — new, open. See "Flipper" above.
- #15 — Flipper: vérification live pas encore faite — new, open. See "Flipper" above.
- #16 (server) — Journals Calculator: mode de prix « EMV » manquant — **closed** (2026-08-29). See "Journals Calculator" above.
- #17 (server) — Journals Calculator: coût de remplissage manuel pour Fishing/Crafting/Mercenary/Trophy Général — new, open. See "Journals Calculator" above.
- #18 (server) — Journals Calculator: vérification live pas encore faite (session signée) — new, open. See "Journals Calculator" above.
- #19 (server) — Craft Finder: vérification live pas encore faite — new, open. See "Craft Finder" above.
- #20 (server) — Craft Finder: coût de transport non modélisé et ville de simulation sans effet numérique — new, open. See "Craft Finder" above.

All issues (server #1-20, client #1-7) reflect this doc as of 2026-08-31 — GitHub is the source of truth for granular tracking, this doc for the "why" behind each.

## Environment gotchas hit on the original dev machine

- **NuGet.Config**: the machine's global `%APPDATA%\NuGet\NuGet.Config` had an *empty* `<packageSources>` list, causing `dotnet restore` to fail with `NU1100` on every single package (even base .NET runtime packs). Not a code issue. Fix: `dotnet nuget add source https://api.nuget.org/v3/index.json -n nuget.org`. Check this first on a new machine if `dotnet restore`/`dotnet build` on `TrimsSilver-Client` fails everywhere at once.
- **Local Postgres without Docker**: `npx prisma dev --detach` (inside `TrimsSilver-Server`) starts a zero-install local Postgres-compatible instance and prints a connection string — put that in `.env`'s `DATABASE_URL`.
- **`prisma migrate dev` fights with `prisma dev`'s auto-sync.** The local `prisma dev` server auto-applies whatever's in `prisma/migrations/` to both the main *and* shadow database on every start/reconnect, but without populating `_prisma_migrations` history — so `migrate dev` reliably fails with **P3005** ("schema not empty") or **P3006/P3018** ("relation already exists" on the shadow DB) even against a supposedly fresh instance. What actually worked: `npx prisma migrate resolve --applied <last-migration-name>` to baseline the history table against the already-applied schema, then generate the new migration's SQL directly with `npx prisma migrate diff --from-config-datasource --to-schema ./prisma/schema.prisma --script > prisma/migrations/<name>/migration.sql` (bypasses the shadow DB entirely), apply it with `npx prisma db execute --file <that file>`, then `npx prisma migrate resolve --applied <name>` again to record it. `prisma migrate reset --force` requires explicit user consent — Prisma itself blocks AI agents from running it unattended.
- **`.env` is gitignored** (correctly) — a new machine needs its own copy from `.env.example`, with a real Discord OAuth app's `AUTH_DISCORD_ID`/`AUTH_DISCORD_SECRET` (create one at https://discord.com/developers/applications if starting fresh, redirect URI `{AUTH_URL}/api/auth/callback/discord`) and a generated `AUTH_SECRET` (`node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"`).
- **`gh` CLI** wasn't installed originally; needed for issue tracking (`winget install GitHub.cli`, then `gh auth login` interactively in a real terminal — Claude Code can't do the browser login step for you).
- **Portainer stack env vars**: see "Deployment" above — use Portainer's stack env var UI, not a `.env` file, for `AUTH_SECRET`/`AUTH_URL`/`AUTH_DISCORD_ID`/`AUTH_DISCORD_SECRET`.

## How to test the client/server link

Two levels: a fast server-only check (no client build, no Discord account needed beyond a browser sign-in), then the real end-to-end client test.

### 1. Prerequisite: redeploy the Portainer stack
`/cli-auth`, `/api/me`, and the 7 ingest routes are pushed to `main` but **won't exist on the live server until the Portainer stack is redeployed/rebuilt** from the latest commit. Do this first, or every step below will 404.

### 2. Server-only check (fastest, proves the whole server side works)
1. Open `https://trimssilver.trimards-island.org` in a browser, sign in with Discord.
2. Open browser dev tools → Console, run:
   ```js
   const r = await fetch("/api/tokens", { method: "POST", headers: { "Content-Type": "application/json" }, body: "{}" });
   console.log(await r.json()); // note the "token" value
   ```
3. In a terminal, validate it and try an ingest call:
   ```bash
   curl https://trimssilver.trimards-island.org/api/me -H "Authorization: Bearer <token>"
   curl -X POST https://trimssilver.trimards-island.org/api/be/globalMultiplier \
     -H "Authorization: Bearer <token>" -H "Content-Type: application/json" \
     -d '{"serverId":1,"globalMultiplier":1.1}'
   ```
   Both should return `200`/`{"ok":true}`. This alone confirms Discord sign-in, token minting, and the ingest pipeline all work in production — without touching the desktop client.

### 3. Real end-to-end test with the desktop client
1. Build & run `TrimsSilver-Client` (`AlbionDataAvalonia.Desktop` or the platform-specific startup project — see `TrimsSilver-Client/AGENTS.md`). It now ships with `TrimsSilverAuthUrl`/`TrimsSilverIngestApiBase` already pointed at the live server (see commits `dcbc19c`, `eadc1ee`).
2. In the running app, go to the main view's **Authentication** section and click **Sign In**.
3. This opens your system browser to `https://trimssilver.trimards-island.org/cli-auth?redirect_uri=http://localhost:5000/`. Sign in with Discord, then click **Autoriser**.
4. The browser tab should show "TrimsSilver sign-in successful. You can close this window." — if instead you get a connection error on `localhost:5000`, the client's `HttpListener` isn't running/reachable (check the client is actually running and nothing else holds port 5000).
5. Back in the client, the Authentication section should switch to the logged-in state (avatar/initials, upload options visible). Client logs (Serilog) should show `User signed in: ...`.
6. To confirm data actually reaches the server: either open the game with the client capturing traffic and watch for successful upload log lines, or just re-run the `curl .../api/me` check from step 2 with the token the client now has stored locally (SQLite `UserAuth` table, `RefreshToken` column) — same validation, proves the client's stored token is accepted.

## Market Prices page (2026-08-18) — done, confirmed working live, including the chart

Added a public-facing `/market-prices` page — the site's first real data-browsing UI (previously just `/` and `/cli-auth`). Modeled loosely on AFM's own Price Checker (screenshots the user supplied), but deliberately **not** a clone: no Intro/marketing tab, no Shareable URLs, no Preconfigured Links (all explicitly declined, not deferred — the screenshots were reference material, not a spec to replicate 1:1). The one piece that *was* genuinely deferred (the per-item historical chart) is now done too — see below.

- **Item catalog**: `scripts/build-item-catalog.mjs` (`npm run catalog:build`) fetches `items.json` + `formatted/items.json` from `broderickhyman/ao-bin-dumps` and extracts one row per tradable item (has a `shopcategory` and a resolvable EN-US name, and isn't flagged `@showinmarketplace=false`/`@hidefromplayer=true` — excludes vanity skins, GvG trophies, quest items) into the committed `src/data/item-catalog.json` — **3,493 items**, ~415 KB. Not wired into CI; rerun manually after major game patches. Served via `GET /api/market/items` (module-scope in-memory cache, not a DB table).
  - Each item carries `enchantSuffix` (`"@"` or `"_LEVEL"`) and `hasQuality` (bool), both **discovered from the actual game data**, not guessed:
    - **Enchant addressing differs by item type.** Equipment enchant variants are a nested `enchantments.enchantment` array on the base item and AODP addresses them as `UNIQUENAME@N`. Resources (raw ore/wood/fiber/hide/rock and refined metal bar/planks/leather/cloth) are different: each enchant level is its **own standalone game item** named `UNIQUENAME_LEVELN` with no localization string of its own — and AODP's actual addressing for those is `UNIQUENAME_LEVELN@N`, both markers combined, confirmed against a working AODP URL the user found. `itemId()` in `types.ts` builds the right one per item.
    - **Quality (Normal..Masterpiece) only applies to equipment/mounts** — resources, farmables, consumables etc. are always quality 1. Driven by `@maxqualitylevel`, present only on `equipmentitem`/`weapon`/`mount` raw-data entries. `PriceGrid.tsx` renders a single "Price" column instead of 5 when `hasQuality` is false.
- **Shop Categories picker** (`CategoryTree.tsx` + `categoryTaxonomy.ts`): a 3-column drill-down (category → subcategory → type), not a flat checkbox list. Raw `shopcategory`/`shopsubcategory1` values are grouped into a friendlier taxonomy (Weapons, Chest/Head/Foot Armor, Off-Hands, Capes, Bags, Mounts, Consumables, Vanity, Gathering Equipment, Artifacts, Farming, Furniture, Materials & Resources, Skillbooks, Trophies, Labourer Contracts, Other). The last level collapses tier-only variation into one row per real "type": gear names follow `{tier rank}'s {line name}` (e.g. "Adept's Broadsword" T4 / "Elder's Broadsword" T8) so stripping the rank prefix groups tiers of the same line together; non-gear items (Ore, Wood, ...) don't have that prefix but get the same treatment when a subcategory's items each sit at a distinct tier (a genuine 1:1 tier progression) — subcategories with many *different* items sharing a tier (e.g. ~90 distinct Furniture decorations mostly at T1) are correctly left ungrouped.
- **Live prices**: `src/lib/aodp.ts` proxies AODP's public stats API (no auth) — `fetchCurrentPrices()` hits `/stats/prices/...`, `fetchAveragePrices()` hits `/stats/history/...` and averages `avg_price`/`item_count` buckets over the requested day window server-side (→ avg price *and* avg amount traded), `fetchPriceHistorySeries()` returns the raw (unaveraged) daily buckets for the chart. Region subdomains (`west`/`east`/`europe`) match the client's `AlbionServers.cs` `serverId` convention. `GET /api/market/prices` (grid) and `GET /api/market/history` (chart) both proxy live, nothing persisted. **This is the live-proxy slice of issue #6** ("AODP reader service") — the fuller caching/storage half of that issue is still open and unbuilt.
- **Favorites**: `MarketPriceFavorite` Prisma model (migration `20260818090000_add_market_price_favorite`), gated on the Auth.js session cookie (`GET/POST /api/market/favorites`, `PATCH/DELETE /api/market/favorites/[id]`) — browser-only, unrelated to the desktop client's bearer-token ingest auth. Price Checker itself stays open to anonymous visitors, matching AFM; only saving/loading favorites requires Discord sign-in.
- **Price grid**: enchant variants of the same tiered item group under one collapsible card-row header (e.g. "Hunter Shoes (5)"); each city is a solid color-coded row band (`CITY_ROW_STYLE` in `types.ts`, matching the real in-game city colors) rather than a text label, with column separators between quality cells; each cell shows the headline sell/buy price, average price + average amount traded, and an hours-old freshness badge (inline, not overlapping the price). Cards size to their own content and wrap in a flex row, so they pack side by side and reflow responsively instead of stacking full-width. Prices load automatically the first time an item is selected; every change after that is manual via **Refresh Prices**.
- **Price history chart** (`PriceHistoryChart.tsx`, server issue #7 — now closed): click any price cell to open a line chart of that exact item/city/quality's last 30 days, right below the table. Hand-rolled SVG (no charting dependency, single series) — 2px gold line, ~10% opacity area wash, dashed muted average-reference line, hairline gridlines, hover crosshair + tooltip (value leads, date/amount follow), built per the dataviz skill's mark/interaction specs. Uses a `key`-per-selection remount rather than a reactive-deps effect (documented inline) since the React Compiler's `set-state-in-effect` rule couldn't be satisfied any other way for this one.
- **Theme**: permanent navy/gold palette (`--color-navy-*`/`--color-gold-*` tokens in `globals.css`, dark-only — no more OS light-mode toggle). New shared `Header.tsx` (slim top bar: wordmark + auth status) and `Sidebar.tsx` (left-column site nav — Home, Market Prices, active-route highlighted via `usePathname` — AFM-style layout, but only the site's actual pages, not AFM's full menu of unrelated features), both wired into `layout.tsx`. Replaced the duplicated sign-in UI that used to live only on the home page and the old inline top-bar nav link.
- UI lives under `src/app/market-prices/` (`MarketPricesApp.tsx`, `ItemPicker.tsx`, `CategoryTree.tsx`, `PriceGrid.tsx`, `PriceHistoryChart.tsx`, `FavoritesTab.tsx`, `categoryTaxonomy.ts`, `types.ts`, `actions.ts`), plus site-wide `src/app/Header.tsx` and `src/app/Sidebar.tsx`.
- **Confirmed working live in production** by the user after redeploy, and separately verified end-to-end in this session via a real browser (Playwright MCP, set up mid-session — see "Playwright MCP" below): category tree, item selection, populated price grids (including enchanted resources), and the price history chart (cell click → correct chart, hover crosshair/tooltip, close button) all confirmed working with zero console errors.

### Playwright MCP (browser access for this agent)

Set up so Claude Code can actually see/drive the site instead of guessing from source. Config lives at the **workspace root**, not inside a project's `.claude/` folder: `e:\Github\Albion\.mcp.json` (sibling to `TrimsSilver-Server/` and `TrimsSilver-Client/`):
```json
{ "mcpServers": { "playwright": { "command": "npx", "args": ["@playwright/mcp@latest", "--browser=chromium"] } } }
```
`--browser=chromium` is required on this machine — the default `chrome` channel isn't installed and installing real Google Chrome needs admin rights. Playwright's own bundled Chromium works without admin: `npx playwright install chromium`, then `npx @playwright/mcp install-browser chrome-for-testing` (the exact build the MCP server's bundled Playwright core wants — it printed the right command when it failed). After editing `.mcp.json`, the running MCP connection needs a reconnect to pick up new args — `/mcp reconnect` didn't work in this VSCode extension session, but it self-recovered/reconnected on its own shortly after.

## Farming & Breeding Calculator (2026-08-18) — done, confirmed working live via browser

Added a public-facing `/farming` page, the second real data-heavy feature after Market Prices. Modeled on AFM's own Farming and Breeding Calculator (screenshots the user supplied: Intro + Settings tabs), again deliberately **not** a clone of the Intro/marketing tab — but this time scoped for **full parity** with AFM's Settings tab (location bonuses, Premium/tax/fee, market locations, and a Specs/character system), per explicit user direction.

- **Farming catalog**: `scripts/build-farming-catalog.mjs` (`npm run farming-catalog:build`) pulls `items.json`, `loot.json`, and `achievements.json` from `ao-bin-dumps`, plus `localization.json` (raw TMX, 58 MB, no `formatted/` equivalent exists for it) to resolve spec display names. Writes `src/data/farming-catalog.json`: 15 plant recipes (8 crops + 7 herbs), 18 animal recipes (chicken/goat/goose/sheep/pig/cow + horse + ox, tiers 3-8), 21 food items, and 27 farming spec definitions.
  - **Real per-item mechanical data, not estimates**: seed→crop pairing resolved through `loot.json`'s lootlists (main output + secondary-chance bonus drops, e.g. Carrot Seeds → 3-6 Carrots + 10% chance of 1 Earthworm), baby→grown animal pairing + recurring product loot (e.g. Chicken → 8-11 Eggs every 22h), grow time, base focus cost (`@activefarmfocuscost`), max nurture bonus (`@activefarmbonus`), base seed/offspring return chance (`harvest.seed.@chance` / `grownitem.offspring.@chance`), and food consumption (nutrition max, seconds/nutrition, food category) — two separate consumption contexts exist per animal (growing the baby vs. feeding the grown animal for products), captured separately.
  - **Farming specs come straight from `achievements.json`'s `templateachievement` bucket** (`@category === "farming"`, ids like `FARM_CROPS_CARROT`) — cross-referenced 1:1 against every spec name in the user's own screenshot (Crop Farmer/Carrot Farmer/..., Animal Breeder/Chicken Breeder/..., Herbalist/Agaric Cultivator/...), all matched exactly.
  - **Beast/rare-mount breeding excluded** (Direwolf, Swamp Dragon, faction-war variants, etc.) — no clean AODP sell path, several faction/territory-locked; only standard Royal-tradeable livestock + horses/oxen are in scope.
- **Specs auto-populate from data the desktop client already uploads — this was a real correction, not an assumption.** Initially planned as manual-entry-only (no equivalent to AFM's paid Data Client integration); the user flagged that TrimsSilver-Client already reports this, which checked out: `FullAchievementInfoEvent`/`AchievementsService` in the client already captures Destiny Board levels (0-100) and uploads via `TrimsSilverUploader.UploadAchievements()` → `POST be/achievements`, gated by `UploadSpecsToTrimsSilver` (**default on**). The server already persisted this via `AchievementSnapshot`/`AchievementEntry` (existing models, no migration needed) — `GET /api/farming/specs` reads the signed-in user's own snapshots, filtered to `FARM_*` ids, keyed by character (a user can have multiple synced characters, matching AFM's own Character dropdown). Manual override/entry stays available as a fallback.
- **Location bonuses**: `src/data/farming-constants.ts` hand-maintains the 6-Royal-city +10% yield table (Brecilien bonuses all crops, Caerleon gets 3 herbs) — sourced from wiki.albiononline.com/wiki/Farming (fetched via the Playwright browser; `wiki.albiononline.com` 403s WebFetch's plain fetcher) and cross-checked against the user's own Lymhurst screenshot (Carrot Seeds/Crenellated Burdock Seeds/Gosling/Pumpkin Seeds, all +10% — matched exactly). Outlands farm territories (which use per-territory biome/quality bonuses instead) are explicitly out of scope — no small stable table exists for that the way it does for the 6 Royal cities.
- **Calculation engine**: `src/app/farming/calc.ts`, pure functions decoupled from React/Next — `focusCost()`/`nurtureBonus()` linearly interpolate between the recipe's own base cost (spec 0) and a universal floor of 125 (spec 100), since the wiki documents only the two endpoints, not the curve shape (documented approximation). `specLevelForRecipe()` combines the category-wide spec (e.g. "Crop Farmer") and the item-specific spec (e.g. "Carrot Farmer") by taking the higher of the two — the achievement data has independent weights for each (1 vs 2) but doesn't document how they combine, so this is also a documented approximation. `evaluateRecipe()` returns cost/revenue/profit per cycle and per day, profit/focus, and fame/day, given a price-lookup callback (source-agnostic — same functions work for AODP, EMV, or manual prices).
- **Pricing**: reuses `/api/market/prices` unchanged (no new prices route needed) — same AODP proxy Market Prices already uses, called with the farming item set and `qualities=1` (farmables are always quality 1). Sell-side prices have Premium-derived sales tax + market setup fee deducted (`salesTaxRateFor`/`setupFeeRateFor` in `types.ts`); buy-side prices don't (Albion doesn't tax buyers). Four price modes: AODP Current, AODP Average, **My EMV** (signed-in only — reuses the existing private `ItemEstimatedMarketValue` ingest data via the new ownership-scoped `GET /api/farming/emv`), and Manual (inline per-item overrides in the Results table's expanded row).
- **Persistence**: `FarmingCalculatorSettings` Prisma model (migration `20260818110000_add_farming_calculator_settings`) — one row per user (`@unique` on `userId`, upsert), unlike Market Prices' named-favorites list, matching AFM's own single "Save Settings" control. `GET/PUT /api/farming/settings`, gated on the Auth.js session cookie.
- **UI**: `src/app/farming/` — `page.tsx`, `FarmingApp.tsx` (Settings/Results tabs, no item-picker — Results computes across the whole catalog by default, sortable), `SettingsPanel.tsx` (location + bonus cards, Premium/tax/fee, market/price-mode config, Character selector + grouped spec inputs), `ResultsTable.tsx` (sortable table, expandable rows showing focus cost, cheapest-food breakdown, missing-price warnings and manual-price inputs). Added to `Sidebar.tsx`'s nav alongside Market Prices.
- **Real bug caught and fixed during verification**: the loot-amount parser split `"@amount"` on `-` and destructured `[min, max]`, but single-value amounts (e.g. Earthworm's `"1"`) leave `max` as `undefined` — and `Number.isNaN(undefined)` is `false` (only real `NaN` triggers it), so the fallback-to-min never ran and `undefined` silently propagated through `JSON.stringify` (key just vanished) and then through `averageAmount()`'s `(min+max)/2` as `NaN`, making every crop/herb row's revenue and profit show `NaN` in the live UI (all of them have at least one single-amount bonus drop). Fixed by checking `parts.length` instead of `Number.isNaN`.
- **Confirmed working live via Playwright MCP** in this session: Settings tab renders location bonus cards matching the user's screenshot exactly; Results tab loads real AODP prices with correct sort/expand; setting a spec to 100 correctly dropped focus cost 1000→125 and multiplied profit/focus accordingly; switching to Manual price mode zeroed out then live-recalculated on entry with the missing-price field list updating dynamically. **Not exercised in this session** (needs the user's own Discord sign-in): specs auto-fill from a real synced character, and Settings save/load round-trip — both implemented and type-checked against the real schema, but not clicked through with a live session.

### Follow-up fixes and refinements (later on 2026-08-18)

A second round, driven by the user actually using the tool and — critically — cross-checking it against real in-game screenshots, which caught two genuine formula bugs no amount of wiki-reading would have surfaced.

- **Results split into three sections** (Agriculture/Plantes/Élevage in `ResultsTable.tsx`) instead of one flat table, each independently sortable, with fixed identical column widths (`table-fixed` + shared width classes) so the columns line up vertically across all three.
- **"Use Focus" toggle** (`FarmingConfig.useFocus`): off means results use only the tier base yield and location bonus, zero Focus cost — no spec bonus applied at all.
- **NPC merchant price fallback**: every seed/baby has a fixed NPC buy price (`craftingrequirements.@silver` in the raw item data, matches the wiki's "Price Per Seed/Offspring" column exactly) — now used whenever no live market price exists, with a small "NPC" badge and the source spelled out in the expanded row ("Input price: X silver (NPC merchant/Market)").
- **Two real formula corrections, found by the user comparing the tool's numbers against the actual in-game farm-plot tooltip** (Firetouched Mullein Seeds at Thetford, two screenshots — watered vs. not):
  1. The Royal-city "Local" +10% bonus applies to **output amount** ("Production de produits" — the harvest quantity), not to seed/offspring return chance ("Production de graines") as originally modeled. Moved from `returnFraction()` to `averageAmount()`.
  2. The watering/nurture yield bonus (`@activefarmbonus`) is **not scaled by spec level** — it's a flat amount granted in full whenever watering happens; the screenshot's "Arrosage: +17.78%" matched the raw item value exactly, not some fraction of it. Only the Focus *cost* of watering scales with spec (1000→125, confirmed accurate against the same screenshot: a ~91-spec character's cost matched the linear formula almost exactly). `nurtureBonus()`/spec-scaling removed from `returnFraction()`.
  - Also brought animal breeding in line with the crop model: baby→grown animals without a recurring product were valuing expected offspring as *revenue* (at baby price) instead of discounting the *input cost*, unlike seeds. Now symmetric — the return chance only ever discounts cost, never adds to revenue, matching the user's stated principle ("I only sell the product").
  - Every row's expanded detail now spells out the exact math (return chance, net units bought/cycle, input price + source) so this class of question is self-answering from the UI.
- **Production slots** setting (`FarmingConfig.slots`, default 9 — "1 field/pasture = 9 slots") drives a new **Total Profit/Day** column (`profitPerDay × slots`) for an actual-earnings estimate, not just per-slot comparison.
- **Price staleness indicator**: prices ≥12h old (current-mode only — averages/manual/EMV have no single "age") get a red "⟳ Xh" badge on the row, plus a compact "⟳ N items need a refresh" badge next to the Refresh Prices button whose hover tooltip lists every stale item and its age (iterated from an initial always-visible banner, which the user found too space-hungry — a hover summary was the better fit).
- **Deliberately deferred (noted by the user, not filed as a blocker)**: some animals can be raised for more than one purpose in-game — e.g. Geese for meat (sell the grown animal) vs. for eggs (keep feeding for the recurring product) — and the calculator currently only ever shows one row per animal (the product cycle when one exists, otherwise the growth cycle), picking a side rather than showing both as separate comparable rows. Tracked as server issue #9.

## Crafting Calculator (2026-08-24) — done, ships alongside Farming as the third data-heavy page

Added a public-facing `/crafting` page: item picker + enchant/quality selection, live AODP resource/output pricing, and a cost/revenue/profit/ROI breakdown per craft. Modeled on AFM's craft-calculator-simple equipment-crafting scope, not a blanket "anything craftable" tool.

- **Crafting catalog**: `scripts/build-crafting-catalog.mjs` (`npm run crafting-catalog:build`) pulls `items.json` from `ao-bin-dumps` and extracts **768 weapons/armor/bags/capes/offhands** (`weapon` + `equipmentitem` buckets only — deliberately excludes `consumableitem`/`mount`/`furnitureitem`/`journalitem`, which also carry `craftingrequirements` but are Farming's or out of scope) into `src/data/crafting-catalog.json`. Each item's base `craftingrequirements` becomes its enchant-0 recipe; each `enchantments.enchantment[]` entry (levels 1-4) carries its own recipe. No names/icons stored here — the UI joins against the existing `item-catalog.json` by `uniqueName`. `@combatspecachievement` (present on ~1040 of the ~1480 in-scope items) is captured as `specAchievementId` for spec auto-fill, same mechanism as Farming's `FARM_*` ids; items without it just skip auto-fill.
- **AODP addressing**: `craftItemId()` in `calc.ts` reuses the same `UNIQUENAME@N` convention as Market Prices/Farming, confirmed to work uniformly for both the crafted output and recipe resources at enchant>0 (resources already carry their own `_LEVELN` infix from the raw game data).
- **Calculation engine** (`calc.ts`): `evaluateCraft()` is pure and source-agnostic like Farming's `evaluateRecipe()`. Resource Return Rate discounts net units consumed per craft (`count * (1 - returnRate)`); station fee is a percentage of resource cost; a flat silver fee from the recipe is added on top. Revenue is the output's sell price (net of sales tax/setup fee, same convention as Farming). Returns cost/revenue/profit per craft and per batch, ROI, and profit-per-Focus.
- **Two things deliberately left as manual inputs, not modeled constants** (`crafting-constants.ts`): **Resource Return Rate** (defaults to 0, not a guessed "typical" number — the wiki doesn't publish a clean per-category table the way Farming's location bonuses were documented, so the user reads their actual in-game value and types it in) and **station fee rate**. A `returnRateIncludesStationBonus` checkbox is pure record-keeping (reminds the user whether their typed Return Rate already accounts for a city bonus) — it has no separate effect on the calculation, since no city→bonus-category table is hardcoded. Tracked as server issue #10.
- **Focus is shown, not applied as a cost reduction**: the raw per-craft Focus cost comes straight from game data (`recipe.focusCost`) and is displayed as-is (`useFocus` toggle just hides/shows it, same UX pattern as Farming). Focus Cost Efficiency from specialization mastery is a multi-node formula the wiki documents as a scrollable table rather than a clean 0-100 curve like Farming's watering bonus, so it was judged too risky to model precisely and is deliberately not applied.
- **Extracted shared constants**: `market-constants.ts` now holds `DEFAULT_SALES_TAX`/`DEFAULT_SETUP_FEE` (pulled out of `farming-constants.ts`) so both calculators reuse the same premium/standard tax and setup-fee rates.
- **Pricing**: reuses `/api/market/prices` (same AODP proxy as Market Prices and Farming) with three modes — AODP Current, AODP Average, Manual. **No "My EMV" mode here** (unlike Farming) — the private `ItemEstimatedMarketValue` data is stored per quality-1 only today, which is correct for farmables but wrong for equipment that varies quality 1-5; wiring that correctly is a deferred follow-up, not a V1 blocker. Tracked as server issue #11.
- **Persistence**: `CraftingCalculatorSettings` Prisma model (migration `20260824220000_add_crafting_calculator_settings`), mirroring `FarmingCalculatorSettings` — one row per user, upsert. `GET/PUT /api/crafting/settings`, gated on the Auth.js session cookie. `GET /api/crafting/specs` reads the signed-in user's `AchievementSnapshot`/`AchievementEntry` rows for combat spec auto-fill (same mechanism as Farming's `/api/farming/specs`, different achievement id namespace). `GET /api/crafting/recipes` serves the catalog (module-scope in-memory cache, like `/api/market/items`).
- **UI**: `src/app/crafting/` — `page.tsx`, `CraftingApp.tsx` (region/city/premium/price-mode config, item+enchant picker, Return Rate/station fee/Focus inputs, Character selector), `ItemRecipePicker.tsx`, `ResultPanel.tsx` (resource cost breakdown, revenue, profit, ROI, profit-per-Focus). Added to `Sidebar.tsx`'s nav alongside Market Prices and Farming.
- Commit `996e11c` on `main`. Not yet separately confirmed live via a real browser session the way Market Prices and Farming were (type-checks clean, same patterns as the two shipped calculators) — tracked as server issue #12.

## Price refresh "Unexpected token '<'" bug fix (2026-08-18)

User-reported live: refreshing prices on Market Prices and/or Farming occasionally failed with `Failed to load prices: Unexpected token '<', "<!doctype "... is not valid JSON`. Two combined issues, both in code that predates this session:

- `src/lib/aodp.ts`'s three AODP `fetch` calls (current prices, averages/history, single-item history) had no timeout. A slow/hanging upstream request could run until the *hosting platform's own* gateway/function timeout killed it and returned an HTML error page — bypassing this file's own `try/catch`, which only ever returns clean JSON for errors it can see. Fixed with a 15s `AbortSignal.timeout(15_000)` on all three, so we fail fast with our own JSON error before any platform-level timeout can intervene.
- `FarmingApp.tsx`, `MarketPricesApp.tsx`, and `PriceHistoryChart.tsx` all called `res.json()` unconditionally on the fetch response, so any non-JSON body (that HTML error page, or anything else) threw a raw `SyntaxError` that got displayed to the user verbatim instead of a readable message. Fixed with a shared `readJsonResponse()` helper (`src/lib/http.ts`) that checks `content-type` before parsing and throws a clear "try again in a moment" message otherwise.

Live-reproduced only the fast-path (confirmed `/api/market/prices` returns clean JSON for both a single item and the full ~78-item Farming catalog against the live deployment) — the HTML-response path itself is a timing-dependent platform behavior, not something reliably reproducible on demand, so the fix targets the two structural gaps that could produce it rather than a specific reproduction. Commit `5ddd874`, pushed to `main`.

## Flipper (2026-08-27) — done, verified against seeded DB data AND live public data; a real client-side bug found blocking private flips

Added a public-facing (but sign-in-required) `/flipper` page — TrimsSilver's answer to AFM's Black Market Flipper. Finds instant-profit flips (buy from a sell order in one market, sell straight into a buy order elsewhere) computed **entirely from the signed-in user's own scanned `MarketOrder` data** — no AODP call, no new ingest work, since `/api/flipperOrders` and the `MarketOrder` model already existed for exactly this (see "Private data model" above). Scoped down from AFM's full feature set by explicit user decision before building:
- **Private data only** — no cross-user "Public Flips" aggregation via `MarketOrder.contributeToPublic`, even though that flag already exists in the schema. Server issue #13.
- **Stateless V1** — no "mark as consumed" tracking (no new Prisma model, no multi-user visibility question) and no hourly-value statistics chart. A flip just stops appearing once the underlying orders age out or change on the next scan/refresh. Server issue #14.
- **Both Black Market and city-to-city flips**, not just AFM's Black-Market-only scope — a `destLoc.isBlackMarket` flag tags each opportunity so the UI can filter/label the two separately (`showBlackMarketFlips`/`showCityToCityFlips` checkboxes), rather than needing two separate calculators.

- **Location id table, newly ported to the server** (`src/data/market-locations.ts`) — TrimsSilver-Server had no server-side city/location mapping before this (only AODP's *name*-keyed city list in `market-prices/types.ts`). The client uploads `MarketOrder.locationId` **raw and unnormalized** (confirmed via an Explore agent reading `AuctionGetOffersResponseHandler.cs` etc. and the live `world.json` from `cdn.albionfreemarket.com`) — a numeric market id for the 8 canonical markets (`"3003"` = Black Market, `"3005"` = Caerleon city market — genuinely distinct ids, not the same location; raw `world.json` mislabels 3003's own `UniqueName` as `"Caerleon"` too, a legacy game-data quirk, not a bug in this table). `resolveLocation()` handles the 8 canonical ids plus the common Black-Market-entrance variants (`BLACKBANK-<id>` prefix, `<id>-HellDen` suffix, literal `BLACK_MARKET`/`BLACKMARKET` tokens) seen in the client's own `AlbionLocations.cs`, but doesn't attempt its full candidate-resolution table — unresolved ids degrade gracefully (shown as `Unknown (<id>)` in the UI) rather than being dropped. **Not verified against real scanned data** — server issue #15.
- **Matching engine** (`src/app/flipper/calc.ts`, pure/source-agnostic like the other calculators' `evaluateX()`): for each (item, quality, enchant) group, keeps only the single cheapest fresh "offer" (sell order) and single priciest fresh "request" (buy order) per location — no order-stacking in V1 — then evaluates every source→destination location pair. A flip fulfills two *existing* orders rather than placing a new one of your own, so unlike Crafting/Farming's sell price there's **no Setup Fee**, only Sales Tax on the sell leg (buyer never pays tax or fee — same convention already established elsewhere in this codebase). Freshness windows default to AFM's own documented values (offers trusted 180min, requests 45min, both user-adjustable) to avoid flips built on since-filled/cancelled orders.
- **API**: `GET /api/flipper/orders` — Auth.js session-gated (browser cookie, not the bearer-token ingest auth), returns the signed-in user's own `MarketOrder` rows from the last 24h (well beyond the freshness windows above; older rows are typically long-expired/filled orders never rescanned, not useful signal) across all servers, serialized with BigInt fields converted to plain numbers. Client-side filters to the selected region's `serverId`, same "fetch once, recompute locally on every settings change" shape as Farming/Crafting.
- **UI**: `src/app/flipper/` — `page.tsx`, `FlipperApp.tsx`, `FlipResultsTable.tsx` (sortable flat table — Item/Quality/Route/Buy/Sell/Qty/Profit/ROI — with an expandable row showing both raw order ids, each side's scan age, and the item's raw `itemTypeId`). Added to `Sidebar.tsx`'s nav.
- **No new Prisma model, no migration** — the V1 scope decisions above (private-only, stateless) mean this ships entirely on top of data structures that already existed.
- **Real item-name resolution gap, documented not fixed**: `FlipResultsTable.tsx`'s `resolveItem()` strips a trailing `_LEVELN` from `itemTypeId` before falling back to a catalog lookup, to handle enchanted resources (their own standalone in-game item, not a separate `item-catalog.json` row — see Market Prices' `itemId()` for the same convention in reverse). This is a reasonable inference from how AODP addressing already works elsewhere in this codebase, but **unverified against a real scanned resource order** — same open question as #15.
- **Verification**: build + lint + typecheck all clean. Functionally verified by seeding synthetic `MarketOrder` rows directly via SQL (`prisma db execute`) under a manually-inserted `Session` row (cookie set directly in a browser tab — no real Discord OAuth), covering: a profitable Black Market flip, a profitable city-to-city flip, a deliberately unprofitable pair (correctly excluded), and a stale request past the default 45min window (correctly excluded). Profit/ROI math confirmed exactly by hand. BM/city-to-city filter toggles, region switching (empty state for a region with no data), row-expand detail, and the signed-out gate all confirmed working in a live local dev server via the Playwright/Claude Browser tooling. **Not yet exercised with a real Discord session or real client-scanned game data** — server issue #15, same pattern as the Crafting Calculator's #12.
- Workspace-root `E:\Github\Albion\.claude\launch.json` added (`npm --prefix TrimsSilver-Server run dev`) so a preview server can be started directly from the workspace root going forward — didn't exist before this session.

### Client bug found while testing live: private flips need a non-persistent client toggle re-enabled every session

The user tested `/flipper` live on the deployed server immediately after this shipped and got "0 scanned orders" despite the client running and the market being open in-game. Investigated via an Explore agent reading `TrimsSilver-Client`:

`Network/Uploader.cs`'s `Upload(MarketUpload)` (`Network/Uploader.cs:54-125`) only calls `TrimsSilverUploader.UploadMarketOrder` (the `POST flipperOrders` call) when `PlayerState.UploadToTrimsSilverOnly` is `true` (`Network/Uploader.cs:63-82`). This backs the **"Private Flips Mode" checkbox** in `Views/MainView.axaml:298-300`, and it:
- **Defaults to `false`** (`State/PlayerState.cs:22`).
- **Is not persisted anywhere** (`UserSettings`/`AppSettings`) — pure in-memory `PlayerState`, resets on every app restart.
- **Is explicitly forced back to `false` on every logout** (`AuthService.cs:322`) — predates the Discord auth rewrite, still present after it.

So today, a user has to remember to check "Private Flips Mode" *every session*, and it silently un-checks itself on sign-out/sign-in — nothing in the UI reminds them. This is a genuine client bug/gap (not a server issue), tracked as **client issue to be filed** (not yet filed — see Next steps). Confirmed this doesn't route through `FeatureFlags.AfmIntegrationEnabled` (unrelated, only gates the AFM Portfolio/Legendary/EMV features) and isn't referenced anywhere in client git history by name, consistent with this path having been wired up long ago for the old AFM private-upload feature and never revisited.

**Workaround for the user right now**: sign in, open the market UI (or Black Market) in-game to trigger the auction packets, and make sure "Private Flips Mode" is checked in the client sidebar for that session.

### Public Flips added same day, in response to hitting the above live

Rather than block on the client fix, added a second, independent data source to the same `/flipper` page: **Public Flips**, computed from AODP's public current-price snapshot (the same `GET /api/market/prices` proxy Market Prices/Farming/Crafting already use) — no sign-in, no client scanning, no private data needed at all.

- **`findPublicFlips()`** (`calc.ts`) — deliberately a separate function from `findFlips()`, not a unified abstraction: AODP only reports the *best* sell/buy price per city (no orderId, no available quantity), so there's no meaningful "total profit" the way a real scanned order has — only profit-per-unit and ROI. Also no `resolveLocation()` need — AODP already addresses Black Market by the city name `"Black Market"` (see `market-prices/types.ts`'s `CITIES`), same as any other city.
- Reuses `ItemPicker`/`CategoryTree` from `market-prices/` as-is for item selection (bounded, explicit item scope — the same reason Market Prices/Farming/Crafting never do an unbounded AODP scan) — no new item-picking UI written.
- `PublicFlipResultsTable.tsx` — separate component from the private results table (different columns: no Qty/Total Profit, adds per-side price age and an "Avg/Day" liquidity hint from AODP's average-amount data when available).
- The page's sign-in gate was **relaxed**: `FlipperApp.tsx` no longer hard-gates the whole page. Private Flips section shows an inline Discord sign-in prompt when signed out; Public Flips is always usable, matching Market Prices/Farming/Crafting's existing "anonymous-usable" precedent that the original V1 Flipper had broken from.
- **Verified live against real production AODP data** (not seeded/synthetic) in a local dev server: selected several Rune items, hit Refresh, got real profitable flips back — e.g. Adept's Rune Fort Sterling → Caerleon, 7→14 silver net, 105.7% ROI, confirmed against the raw `/api/market/prices` JSON response by hand. Also confirmed the *negative* case is correct, not a bug: T4 Bag showed zero flips because real current spreads across all 8 cities genuinely don't clear the 4% sales tax right now — checked the raw price numbers by hand to confirm no profitable pair was missed.

## Journals Calculator (2026-08-29) — done, verified live against real production AODP data

Added a public-facing `/journals` page, modeled on AFM's own Journals Calculator (browsed live at albionfreemarket.com — Tutorial text, Settings fields, and the Journal Type × Tier results grid + click-to-expand "Journal Run Details" panel were all inspected directly, not guessed). Scoped like Crafting: full parity on the mechanics that are real, documented game data, deliberately manual where the underlying game mechanic isn't cleanly derivable.

- **Journal catalog**: `scripts/build-journal-catalog.mjs` (`npm run journal-catalog:build`) pulls `items.json`'s `journalitem` bucket (133 rows = 19 journal families × T2-T8) into `src/data/journal-catalog.json`. Each row's `@maxfame` (fame needed to fill), `@baselootamount` (100%-yield reward multiplier), and `lootlist.loot[]` (the exact weighted reward table — itemname/itemamount/weight, or `silveramount`/weight for Mercenary) are used as-is rather than the wiki's simplified aggregate percentages, since the raw weighted list is the authoritative source those percentages were themselves derived from. Cross-checked several rows by hand against wiki.albiononline.com/wiki/Journal's own tables (fame-required, base-loot-amount) — exact matches.
  - **19 families**, confirmed exhaustive against the wiki's naming-scheme section: 5 raw-resource Gathering (Lumberjack/Stonecutter/Prospector/Cropper/Gamekeeper) + Fishing + 4 Crafting (Blacksmith/Fletcher/Imbuer/Tinker) + Mercenary + 8 Trophy variants (one per gathering type + Fishing + Generalist + Mercenary). Mapped in `src/data/journal-constants.ts` — note the family string doesn't always match the resource's own type (`STONE` journals gather `ROCK`, confirmed by inspecting `T4_JOURNAL_TROPHY_STONE`'s reward, `T4_FURNITUREITEM_TROPHY_ROCK`).
  - **Fill cost is only auto-computed for the 5 gathering families + their 5 trophy counterparts** (10 of 19), via `famefillingmissions.gatherfame.validitem` (the exact list of valid resource items, own tier through +2, all enchant levels) cross-referenced against each resource's own `@famevalue` field (confirmed exactly against the wiki's "Fame per Resource" table, e.g. `T4_WOOD`=7.5, `T4_WOOD_LEVEL1`=15, `T4_WOOD_LEVEL2`=30). Fill units = `maxFame / famevalue`; the UI lets you pick which fillOptions entry to gather (defaults to same-tier, enchant 0).
  - **Fill cost is a manual entry for the other 9 families** (Fishing/Trophy Fishing, the 4 Crafting families, Mercenary/Trophy Mercenary, Generalist Trophy) — deliberately not modeled, not a guessed approximation. Fishing has no single "the" input (any tier, dozens of biome/rarity fish SKUs). Crafting fame per equipment item isn't a field anywhere in `items.json` — confirmed by checking: base-enchant refined resources (`T4_METALBAR`) carry no `@famevalue` at all, only their *enchanted* variants do (`T4_METALBAR_LEVEL1`=45, matching the wiki's separate "enchanted refining fame bonus" patch-note table, not base crafting fame), so there's no clean formula to derive it from. Mercenary/Trophy Mercenary fill from PvE kill fame (not market-priceable) and Generalist Trophy fills from *any* fame-earning activity at all. All 9 still get full delivery/reward-side modeling from the same loot data, so "Buy Full, Sell Mats" works identically for every family — only "Buy Empty, ..." on these 9 asks for a manual fill-cost number.
- **Calculation engine** (`src/app/journals/calc.ts`): `evaluateJournal()` is pure/source-agnostic like the other calculators. Expected reward per SKU = `baseLootAmount * (yieldPct/100) * (entry.weight / totalWeight) * itemAmount` — no separate enchant-distribution constant needed, since the lootlist weights already encode it exactly per tier. Mercenary's "yield above 100% is a chance to receive double, not a continuous scale-up" mechanic (wiki-documented) needed no special-case code — the expected value of that chance mechanic is algebraically identical to linear scaling by yield%, confirmed by hand.
  - **Setup fee is order-side-conditional, not a flat convention like Farming/Crafting**: AFM exposes an explicit Buy/Sell Price Type per leg (Sell Order / Buy Order / Average / Manual), so unlike Farming/Crafting (which always deduct both tax+fee on every sell, no order-type choice), Journals models the real Albion rule — a setup fee is only charged on the side where *you* place a resting order (Buy leg + "Buy Order" price type, or Sell leg + "Sell Order" price type); taking the instant opposite-side order costs no fee. Sales tax always applies on the sell leg regardless of order type. Average/Manual price types are treated as already-realized (tax still applies, no fee).
  - AODP addresses a journal's Full (filled) state as `{uniqueName}_FULL` — a state tag, not a separate item in `items.json` (confirmed no `_FULL` entries exist anywhere in the raw dump) — matched against AFM's own `/pricecheck/T8_JOURNAL_WOOD_FULL` link. `journalMarketId()` in `journal-constants.ts`.
- **Pricing**: reuses `/api/market/prices` (same AODP proxy as Market Prices/Farming/Crafting/Flipper), chunked into ≤100-item batches (`MAX_ITEMS` cap on that route) since the full priced-item set — 133 journals × 2 states + every distinct reward material + every fill-resource option — comfortably exceeds 100, unlike the other calculators' smaller catalogs.
- **Persistence**: `JournalsCalculatorSettings` Prisma model (migration `20260829120000_add_journals_calculator_settings`), mirroring Farming/Crafting exactly — one row per user, upsert, `GET/PUT /api/journals/settings` gated on the Auth.js session cookie. `GET /api/journals/catalog` serves the static catalog (module-scope, like the other `/api/*/recipes` routes).
- **UI**: `src/app/journals/` — `page.tsx`, `JournalsApp.tsx` (Settings panel: Scenario, Amount, T2-T7/T8 Yield %, Region, Buy/Sell Location + Price Type, Premium), `ResultsGrid.tsx` (Journal Type × Tier matrix, click a cell to select), `RunDetailPanel.tsx` (Buy/Sell line-item breakdown with per-line qty/unit price/fee/tax, mirroring AFM's own "Journal Run Details" panel exactly, plus the fill-resource picker and manual-fill-cost input where relevant). Added to `Sidebar.tsx`'s nav.
- **Verified live in a local dev server** (real production AODP data via `npx prisma dev --detach` + `npm run dev`, no seeded/synthetic data): all three scenarios render and compute correctly; spot-checked the math by hand — T3 Lumberjack's Journal reward at 150% yield showed `T3_WOOD` qty 90.000 (60 base × 1.5 — exact), fill cost for the same journal showed 300.00 units of `T3_WOOD` needed (900 maxFame / 3 famevalue/unit — exact) at a real live Caerleon sell-order price. Console clean, no runtime errors. One live-data curiosity confirmed to be a real market artifact, not a bug: T8 Fisherman's Journal showed a ~105M silver result driven by a single fish SKU's absurd 12,111,011-silver "lowest sell order" — a real (if likely troll) live AODP quote for a thin-liquidity item, reproduced identically by hand against the raw `/api/market/prices` response.
- **Deliberately not done / follow-ups**: the two Happiness/Yield % fields are free numeric inputs (0-150) rather than AFM's curated discrete dropdown options — mathematically equivalent, avoids fabricating T8-specific presets that weren't independently verified (only saw AFM's own T8 default of 142.5% before the site's cookie-consent dialog and page state made re-opening that specific dropdown impractical mid-session). Not yet exercised with a real signed-in Discord session (Save/Load settings round-trip) — same gap noted for Farming/Crafting/Flipper above.

### EMV price mode added same day (server issue #16, closed)

Added "My EMV" as a fifth `PriceType` (alongside Sell Order/Buy Order/Average/Manual), gated on Discord sign-in exactly like Farming's own EMV mode — the option doesn't even appear in the Buy/Sell Price Type dropdowns until signed in. New `GET /api/journals/emv` mirrors `/api/farming/emv` exactly (ownership-scoped to the caller's own `ItemEstimatedMarketValue` rows, quality 1 always — no quality param needed, since every journal/reward material/fill resource in this feature is quality 1). Unlike Crafting's EMV gap (equipment varies quality 1-5, EMV is only stored per quality-1 — issue #11), this is a clean fit with no follow-up caveat. Chunked into ≤200-item batches (the route's own `MAX_ITEMS`) via the same `chunk()` helper already used for `/api/market/prices`, since the full priced-item set exceeds that in one request. Verified live: the EMV option is correctly absent from both Price Type dropdowns while signed out, console clean, build/lint/typecheck clean.

### Fixed same day: 429 errors on Journals price refresh (user-reported live in production)

User reported `Failed to load prices: ... status 429, not JSON` when hitting Refresh Prices on the live `/journals` page. Root cause: Journals prices its whole catalog at once (~580 distinct items across 133 journals + reward materials + fill resources — far more than Market Prices/Farming/Crafting/Flipper's smaller, bounded item sets), which needs 6-7 chunked `/api/market/prices` calls (that route's own 100-item cap). Firing them back-to-back with no gap could trip rate limiting — either AODP's own, or a layer in front of the self-hosted app; a 429's body isn't guaranteed to be JSON either way, and `readJsonResponse` surfaces that as a generic error rather than something actionable.

Fixed in `JournalsApp.tsx`'s `fetchPricesBatch()`: a 300ms gap between chunks, plus up to 3 retries with increasing backoff (1.5s/3s/4.5s) specifically on a 429 before giving up — a transient rate-limit hit now self-heals mid-refresh instead of failing the whole thing on the first chunk. Verified live in a local dev server against real production AODP data: all 7 chunks succeeded end-to-end, page renders correctly, console clean.

**Generalized the same day, at the user's request** ("gérer cela globalement pour le site"): the chunk/retry/backoff logic was Journals-only, so any other feature whose item set ever grows past the route's 100-item cap — or that simply gets refreshed at an unlucky moment — had no protection. Extracted it into `src/lib/marketPricesClient.ts`'s `fetchMarketPrices()` and switched every caller of `/api/market/prices` over to it: Market Prices' `doFetchPrices`, Farming's `fetchPrices`, Crafting's `fetchPrices`, Journals' `fetchPrices`, and Flipper's `fetchPublicPrices`. Each caller's own request shape (items/locations/qualities/averageDays) is unchanged — only the transport (chunking + backoff) moved into one shared place, so a future feature just calls `fetchMarketPrices()` instead of reimplementing (or forgetting) this. Verified live: all 5 pages (`/market-prices`, `/farming` Results tab, `/crafting`, `/journals`, `/flipper`) load and fetch prices with zero console errors; `npx tsc --noEmit`/lint/build all clean. **Needs the live Portainer stack redeployed from `main` to take effect in production** — the original 429 was live-only and never reproduced locally, so it's not independently confirmed fixed on the actual live site yet.

**Refined the same day against AODP's actual published docs** (https://www.albion-online-data.com/api/, https://www.albion-online-data.com/developer — the user found and shared these): the 100-item-per-request cap and client-side-only delay/retry were both guesses. The real constraints are a 4096-character URL limit (not an item count — item name length varies a lot, 7 to 33+ chars) and 180 requests/minute + 300/5 minutes, enforced against this whole server's shared outbound IP, not per-feature or per-user. Moved the real fix into `src/lib/aodp.ts` — `chunkItemsByUrlLength()` packs each AODP request to the actual number of items that fits under the real limit, and a module-scope sliding-window rate limiter (`waitForRateLimitSlot()`) gates every AODP call in the file (current prices, average prices, and single-item history) against safety margins under the documented ceilings, so concurrent traffic from different features or different users can't collectively trip the limit — something a per-request client-side retry alone can't guarantee. This also closed a real gap: `fetchPriceHistorySeries()` (used by `/api/market/history`, the price chart) had no rate-limit protection at all before this. `/api/market/prices`'s `MAX_ITEMS` raised 100→500 (now a sanity ceiling, not the real constraint) and `marketPricesClient.ts`'s client-side chunk size raised to match, cutting Journals' round trips to our own server from 6-7 to 2. Verified live via `performance.getEntriesByType('resource')` in the browser (the network-request history tool itself turned out to accumulate stale entries across navigations mid-session, which briefly looked like the fix hadn't taken effect — a real dead end during verification, not a code bug) — confirmed actual chunk sizes are 500 + 78 items, not 100-item chunks.

**The database dump question, answered**: the user also asked whether AODP's public daily/monthly Postgres dumps (`albion-online-data.com/database-europe/`, etc.) could be useful here. They're the AODP maintainers' own full backups — 600MB+ compressed per daily snapshot, monthly `market_history` dumps tens of MB each — a firehose of raw historical order data. Recommended against pursuing this now: it's a heavy, ongoing-sync commitment (schema-matching, storage, re-import cadence) disproportionate to what TrimsSilver's calculators actually need (a handful of specific items' current/average prices), and doesn't fit the site's history docs. If server issue #6's "AODP reader/cache service" is ever built for real, AODP's NATS pub/sub feed (also documented on the developer page — `marketorders.deduped` etc., deduplicated over a 5-minute window) is a much better-fitted alternative worth remembering: a live stream to build a local cache from, rather than a batch download to keep re-syncing.

## Market-price cache added same day (server issue #6, closed) — done, sync logic verified live, DB write path not independently confirmed

The user asked what a cache service would actually buy the site, then asked for it to be built. Added a real background cache instead of the "someday" placeholder issue #6 had been: a sync job (`src/lib/priceCacheSync.ts`) kicked off once per server instance via the new `src/instrumentation.ts` (Next.js's `register()` hook — runs once at boot, before the server accepts requests, confirmed against `node_modules/next/dist/docs` per this repo's own AGENTS.md instruction to check the docs before writing code against this version).

- **Scope: Farming + Journals only, not Crafting or Market Prices.** The first design considered caching all four features' catalogs, but Crafting's is ~3,900 craft-item-ids (768 items × up to 5 enchants) × 5 qualities × 8 cities — **~156,000 rows per region**, and Crafting only ever queries a handful of items at once (whatever recipe is selected), never its whole catalog — caching it all would be mostly-unused bulk writes every 10 minutes for no read benefit. Market Prices has the same "queries whatever the user picks" shape at an even bigger catalog (3,493 items). Farming (78 items) and Journals (578 items) are the opposite: both always fetch their *entire* catalog on every refresh, making them the only two features where a full-catalog cache is actually load-bearing. Combined universe: 656 unique items, quality 1 only (farmables/journal materials never vary quality) — **5,248 rows per region, 15,744 total** across all 3 regions. Crafting and Market Prices keep working exactly as before (unchanged, still live-proxied).
- **Sync**: every 10 minutes, `syncPriceCacheOnce()` re-derives the 656-item universe directly from `farming-catalog.json`/`journal-catalog.json` (small duplicated extraction logic, not imported from the "use client" App components — kept deliberately separate rather than sharing code across a server/client boundary) and calls `fetchCurrentPrices()` (the same rate-limited, URL-length-chunked function added earlier today) once per region, upserting results in concurrency-25 batches (not raw SQL — chunked `Promise.all` of ordinary Prisma `upsert()` calls, consistent with this codebase never using raw SQL in application code). A re-entrancy guard skips a run if the previous one is still going; a `globalThis` flag (same pattern as `src/lib/prisma.ts`'s client singleton) stops Next.js dev-mode HMR from stacking up duplicate `setInterval`s.
- **Read path**: `/api/market/prices` queries the cache first; whatever isn't found (an item genuinely outside the 656-item universe, or the cache not synced yet right after a fresh deploy) falls back to a live AODP fetch for just those items — the exact same live-proxy behavior this route always had, now just a fallback instead of the only path. **Zero client-side changes** — this is entirely behind the existing route contract, so Market Prices/Farming/Crafting/Journals/Flipper's own code didn't need to change at all.
- **A cache-read failure degrades gracefully** rather than 502ing the whole request — found and fixed while testing against this session's flaky local dev database (see below): the route was originally written to let a `findMany()` exception propagate into the same catch block as the AODP fetch, which would have taken the *whole* route down on a DB hiccup even though the route could easily still serve everything live. Now a cache-read exception is caught on its own and just means "treat every item as a cache miss."
- **Verification**: `npx tsc --noEmit`/lint/build all clean. The sync *logic* was confirmed live end-to-end with a debug log temporarily added to `instrumentation.ts`: `register()` fires, correctly computes 656 items × 3 regions, calls real AODP successfully, and the per-region try/catch works (each region's failure logged independently without crashing the process) — confirmed via the sync's own timing log (`"Price cache sync: 656 items × 3 regions in 1852ms"`). The write-then-read-hit cycle against a real Postgres was **not** confirmed at first (this session's local `prisma dev` ephemeral database kept dying mid-session, unrelated to this feature) — **but was fully confirmed later the same day once the user granted SSH access to the actual production host** (see below): `SELECT count(*), count(DISTINCT region), count(DISTINCT city) FROM "CachedMarketPrice"` on the real Postgres returned exactly `15744 | 3 | 8`, matching the predicted row count exactly, with the sync's own log line (`"Price cache sync: 656 items × 3 regions in 14694ms"`) present with no per-region errors.

### SSH access granted, and a real production bug found + fixed the same way (2026-08-29)

The user asked what a "test in the real environment" capability would need, then set one up: a dedicated `claude-deploy` SSH user (key-only, no sudo) on the actual Docker/Portainer host, `docker` group membership only — scoped to inspecting/managing containers, not full root. **Important context discovered on first connection**: this is the user's personal homelab server, not a machine dedicated to TrimsSilver — `docker ps` also shows Nextcloud, Immich, Firefly III, a NetBird VPN, Grafana/Loki, AdGuard, and more, all on the same box behind a shared **BunkerWeb** reverse proxy/WAF. Given that, all SSH usage this session was deliberately scoped to read-only inspection of the `trimssilver-server-*` containers and the shared `bunkerweb-*` container's logs — never touching any other service, never a write/restart/redeploy action, consistent with "shared infrastructure" being a high-blast-radius category.

That inspection paid off immediately: reproducing a real Journals price refresh against the actual public domain (`https://trimssilver.trimards-island.org`, via the Browser pane) failed with `ERR_CONNECTION_CLOSED` — a *different* failure mode than anything reproduced locally all session. Checking BunkerWeb's own container logs (`docker logs bunkerweb-bunkerweb-1`) found the real cause: nginx logged the `/api/market/prices` request with **status `000`** (connection killed mid-request, not a clean HTTP error) plus Lua warnings (`using uninitialized "ctx_ref"/"is_whitelisted"/"reason" variable while logging request`), immediately followed by a `[BADBEHAVIOR] increased counter for IP ... (1/30)` — BunkerWeb's anti-abuse scoring treating the failed request as suspicious, which could eventually throttle/ban a legitimate user's IP if it kept happening.

**This means the *original* 429/"connection issue" the user reported was never actually AODP's rate limit — it was BunkerWeb choking on Journals' long `items=` query string** (~10KB+ for a 500-item chunk) well before the request ever reached AODP. All of this session's earlier AODP-side work (the rate limiter, the URL-length chunking in `aodp.ts`, the price cache) are real, worthwhile improvements on their own merits — good citizenship toward AODP's documented limits, faster Farming/Journals refreshes — but they were solving a problem adjacent to, not identical with, what the user actually hit. Root-caused by checking `grep -rn 'large_client_header\|client_max_body' /etc/nginx/` in the BunkerWeb container: `client_max_body_size` (request *body* size, irrelevant to a GET) was generously configured everywhere, but nothing governs URL/header-line length explicitly, meaning nginx's stock default (`large_client_header_buffers`, 8KB total) — and evidently something in BunkerWeb's own Lua request-processing pipeline — chokes well before a client-supplied 10KB+ URL would ever complete.

**Fixed in `marketPricesClient.ts`**: chunking is now sized by actual URL length (a 2000-char budget, computed from real per-request overhead, not a flat item count) — the same technique `aodp.ts` already uses for AODP's own 4096-char limit, just aimed at a different, tighter, empirically-motivated ceiling. Confirmed against `journal-catalog.json`: Journals' 578-item catalog now chunks into 7 requests (87/116/121/77/66/86/25 items), each comfortably under 2000 characters — versus the previous 2 chunks of 500+78 items that produced 10KB+ URLs. Thanks to the price cache shipped earlier the same day, most of these 7 requests are now fast DB reads rather than real AODP round trips, so the extra request count costs little.

**Deployed and confirmed fixed the same day.** The user redeployed the stack via Portainer themselves (Claude looked into triggering it directly — found the stack is a Portainer git-managed compose project with the checkout living inside Portainer's own internal data volume; mounting that volume to reach it was correctly refused by the safety classifier, since it would expose every other stack's config/secrets on this shared box, not just TrimsSilver's — see [[trimssilver-server-ssh-access]] in Claude's memory. A **Portainer stack webhook** would be the properly-scoped way to let Claude trigger this directly in the future). Post-redeploy verification via SSH:
- App container recreated (`docker ps` showed a fresh start time) and the price cache re-synced cleanly on boot (`"Price cache sync: 656 items × 3 regions in 16155ms"`, no per-region errors).
- Reloaded `/journals` on the real public domain and confirmed via the browser's own `performance.getEntriesByType('resource')` that all 7 chunked `/api/market/prices` requests completed successfully (20-110ms each — served from the price cache, not live AODP — with real transferred bytes, not aborted).
- Checked BunkerWeb's own access log directly: **15 `/api/market/prices` requests, all status 200** — zero status-000 entries, zero `uninitialized` Lua warnings, zero new `[BADBEHAVIOR]` counter increments for this route. The original bug is confirmed fixed at the source, not just "should be fixed" from reading the code.

### Empty-journal pricing fixed — was never a naming bug (2026-08-29)

The user asked whether `T2_JOURNAL_WOOD` (the bare uniqueName, no suffix) was really the right AODP id for an *empty* journal, since price updates worked for full journals but not empty ones. Verified directly against AODP's live API (`europe.albion-online-data.com/api/v2/stats/prices/...`): every bare `T{2..8}_JOURNAL_*` id returns all-zero prices with date `0001-01-01` (never recorded) across every tier and city checked, while every `_FULL` id has real listings. AFM's own site corroborates this — its results page only ever emits `pricecheck` links for `_FULL` ids, never for the empty state.

**Root cause: not a naming bug.** Empty journals aren't player-market goods at all — they're bought from (and presumably sold back to) an NPC vendor for a fixed silver price, which is exactly `craftingrequirements.@silver` from the game data — already captured in the catalog as `emptySilver` (`journal-catalog.json`) but never wired into the calculator. `journalMarketId()`'s "empty" branch was correct; `calc.ts` was just asking AODP for something AODP structurally never has.

**Correction the same day**: the user pushed back with an in-game screenshot — Lymhurst's market genuinely lists empty T2 Lumberjack's Journals for sale, at varying prices (499, 500, 699, 700, 748...) with varying durations/quantities, unmistakably real player sell orders, not an NPC vendor listing. So "not player-market goods at all" was wrong. What's actually going on, reconciling both pieces of evidence: the item **is** tradable player-to-player, but AODP has a genuine scan-coverage gap for it (exhaustively reconfirmed: 3 regions × 7 cities × 5 qualities × 30-day history, zero data everywhere, not a fluke). The two cheapest listings in the screenshot (499, 500) sit right at `row.emptySilver` for T2 (exactly 500, from `craftingrequirements.@silver` — the NPC vendor's fixed instant-buy price) — consistent with players undercutting the vendor price as a ceiling, exactly as you'd expect when a fixed-price NPC alternative always exists.

**Final fix in `src/app/journals/calc.ts`**: both the "Empty journal" buy line and the sell-back-after-turn-in line now try the live AODP price first (`ctx.buyPriceOf(emptyId)` / `ctx.sellPriceOf(emptyId)`) and only fall back to `row.emptySilver` when that's null — future-proof if AODP ever starts covering this item, honest floor estimate in the meantime — and are no longer added to `missingPrices` either way. `JournalsApp.tsx`'s `collectPricedItems()` still requests the empty-journal id (needed for the live-price-first path to ever succeed). Verified live in the dev preview: the "Missing price for:" banner for Lumberjack's Journal T2 lists only `T2_JOURNAL_WOOD_FULL, T2_WOOD`, and the sell line for `T2_JOURNAL_WOOD` resolves to 500 via the vendor-price fallback. `tsc --noEmit`/lint clean. Not yet deployed — per the user's stated preference, they redeploy via Portainer themselves.

## Craft Finder (2026-08-31) — done, verified against real production AODP data (no live browser session)

Added a public-facing `/craft-finder` page ("Quoi fabriquer pour gagner de l'argent"): ranks all craftable equipment by profit margin and, for any item, shows a recursive make-or-buy tree down to raw resources. Requested with a detailed spec up front (granularity rules, liquidity filtering, Premium toggle, per-node Focus, bottom-up optimal-cost propagation) — built via plan mode after a state-of-the-art review of `/market-prices`, `/farming`, `/crafting` confirmed how much was reusable.

- **The missing data piece**: `/crafting`'s own catalog (`crafting-catalog.json`) only ever needed equipment recipes, whose ingredients are refined resources (`T4_METALBAR`) — nothing in the repo captured the refining/transmutation chain down to raw ore/wood/hide/fiber/rock. Confirmed by querying `ao-bin-dumps/items.json` live: both raw and refined resources live in the `simpleitem` bucket, and — unlike equipment — each enchant level is its own **standalone top-level entry** with its own independent `craftingrequirements` (not a nested `enchantments.enchantment[]` array). Some entries carry more than one legitimate non-faction recipe option (e.g. `T5_WOOD_LEVEL1` can be made from `T4_WOOD_LEVEL1` *or* from `T5_WOOD`) — both are kept, the evaluator picks whichever is cheaper. New script `scripts/build-resource-catalog.mjs` (`npm run resource-catalog:build`) extracts this into `src/data/resource-catalog.json` (230 rows, filtering out Faction Warfare token variants), the same live-fetch-from-GitHub pattern as the other `*-catalog:build` scripts.
- **Calculation engine** (`src/app/craft-finder/calc.ts`): `evaluateResourceNode()` is a memoized recursive make-or-buy evaluator (memo key = resource uniqueName, which already fully encodes its own enchant level, so no separate enchant dimension is needed there) with a depth guard; `evaluateEquipmentCraft()` is the flat top layer (equipment recipes never reference other equipment, confirmed by inspection, so no recursion needed above the resource layer); `evaluateFinalItem()` combines the quality-independent craft cost with the quality-specific sell price/liquidity — one call = one (item, enchant, quality) triple, never aggregated, per the spec's explicit requirement. Reuses `/crafting`'s own `craftItemId()` and Journals' `resourceMarketId()` for AODP addressing rather than inventing a third convention. Return Rate/station fee/Focus are manual per-category inputs (one row per resource category + one for equipment craft, in `craft-finder-constants.ts`/`CraftFinderConfig`), not per individual tree node — same reasoning `/crafting` already established (issue #10: no verified formula ties these to spec level) — and not literally one field per node (a full tree can have dozens).
- **Buying compares all 8 cities on both sides** — cheapest sell-order for materials, priciest sell-order for the finished item — per the spec's own explicit method; only the manual Return Rate/station fee/Focus inputs are tied to a "simulation city" selector, which today has no other numeric effect (no city→fee table exists anywhere in this codebase). Transport cost/time between cities is a documented v1 simplification, same class of gap as the Flipper's.
- **Liquidity**: reuses `avgAmount` from `fetchAveragePrices()` (AODP history's `item_count`, already computed — nothing new needed in `aodp.ts`) as the sale-rate signal, with a tier-relative threshold curve (`minSaleRateForTier()`, documented approximation, no published AODP-wide baseline exists to calibrate more precisely) so a T8 item isn't held to a T4 item's volume expectations.
- **Price cache extension, not a new cache table**: `priceCacheSync.ts` already had a documented "add your own `xItemIds()` function" extension point (see `schema.prisma`'s `CachedMarketPrice` doc) — used as designed rather than building anything new. Craft Finder's resource-refining universe (quality 1 only, ~230 items) joins the existing 10-minute sync; its **equipment universe is cached at enchant 0 only** (768 items × 5 qualities × 8 cities ≈ 30k rows/region) on a separate, slower 30-minute cycle (`syncEquipmentPriceCacheOnce`) — enchant 1-4 stay live-proxied on demand, the same cardinality tradeoff already accepted for excluding `/crafting`'s own full catalog from this cache (caching every enchant level too would reproduce that same ~156k-row-per-region problem). This split cadence is also a deliberate step back from the BunkerWeb incident documented above (a large query string alone tripped the reverse proxy's abuse detection before AODP's rate limit ever mattered) — a bigger universe gets a gentler sync schedule, not a more aggressive one.
- **Persistence**: `CraftFinderSettings` Prisma model (migration `20260831150000_add_craft_finder_settings`), one row per user, upsert — identical shape to Farming/Crafting/Journals' own settings models. `GET/PUT /api/craft-finder/settings`, `GET /api/craft-finder/catalog` (serves both `crafting-catalog.json` and `resource-catalog.json` together).
- **UI**: `src/app/craft-finder/` — `page.tsx`, `CraftFinderApp.tsx` (settings incl. per-category Return Rate/station fee/Focus table, tier/quality/liquidity filters), `RankingTable.tsx` (sortable, same `SortKey`/`toggleSort` pattern as Farming's `ResultsTable.tsx`), `MakeOrBuyTree.tsx` — a genuinely new recursive expandable component (no precedent existed in this codebase; every prior "expand" was a single-level row toggle). Added to `Sidebar.tsx`'s nav.
- **Migration note for future sessions**: hit the documented `prisma dev`/shadow-DB gotcha again this session (P3006/P3018) plus a self-inflicted complication — an initial `migrate resolve --applied` baseline was run before verifying the local ephemeral dev DB actually had the prior schema applied, which it didn't (only Auth.js's own tables existed), so the recorded history briefly lied about the real schema. Fixed by resetting the local dev DB's `public` schema (user-confirmed first, since it's a destructive op even on a disposable local DB) and re-running `migrate deploy` for real before baselining. Lesson for next time: verify the actual DB schema (`information_schema.tables`) before trusting `migrate resolve --applied` on an unfamiliar local dev DB, not after.
- **Verification**: `npx tsc --noEmit`/lint/`next build` all clean. `resource-catalog.json` spot-checked against raw game data by hand (`T4_METALBAR`, `T4_METALBAR_LEVEL1`, `T5_WOOD_LEVEL1`'s two options, `T4_ORE_LEVEL1`'s transmutation recipe — all exact matches). Verified live against real production AODP data via a local dev server: `/api/craft-finder/catalog` serves 768 equipment + 230 resource rows; `/api/market/prices` correctly live-proxies raw leaf resources not in any cache (e.g. `T2_HIDE`) alongside cached/live refined resources in one response. **Hand-reconstructed a full T4 sword's make-or-buy chain from real fetched prices** (T2→T3→T4 metal bar and leather, each level's buy-vs-craft choice computed by hand with the same formulas `calc.ts` uses) and confirmed it matches the engine's design exactly — at real prices that day, buying beat refining at every level (expected with 0% Return Rate/station fee), and the final sword showed a small real positive margin after tax/setup fee. **Not verified via a live browser session** (no Playwright MCP connection available this session) — same gap already open for `/crafting` (issue #12) and the Flipper (issue #15); filed as server issue #19. Transport cost and the simulation city's lack of numeric effect filed as server issue #20.

### Real bug found live (2026-08-31): "Ventes/jour" column always empty

User-reported live on the deployed site immediately after shipping: the liquidity/sale-rate column was always blank regardless of item. Root cause in `CraftFinderApp.tsx`'s `fetchPrices()`: it copied `/crafting`'s own `const averageDays = config.priceMode === "average" ? config.averageDays : undefined` pattern verbatim — correct for `/crafting` (which has no liquidity concept, `averageDays` there only matters when the user explicitly wants averaged prices), but wrong here, since Craft Finder's sale-rate signal (`avgAmount`, from `/api/market/prices`' own average-prices call) must be available **regardless** of which price mode is used to value the buy/sell prices themselves — the default price mode is "current", so `averageDays` was silently `undefined` on every normal page load, and the server route only computes averages/avgAmount when `averageDays && averageDays > 0`.

Fixed by always passing `config.averageDays` (not conditional on `priceMode`). Trade-off worth knowing: this means every Craft Finder price refresh now also live-fetches AODP history data for the whole item universe (768+ equipment ids × 230+ resource ids), not just current prices — averages aren't cached by `priceCacheSync.ts` (documented there as "not practical to pre-cache for every possible day-window value"), so this is real added AODP load on every refresh, not just in "average" price mode. Relies entirely on the existing rate-limiter/URL-length-chunking infrastructure in `aodp.ts`/`marketPricesClient.ts` to stay safe — not separately stress-tested against this specific larger load pattern yet.

## Next steps, in the order they were being tackled

1. **Decide the fate of Portfolio / Legendary marketplace / AFM EMV lookups** — client issue #5, currently disabled via `FeatureFlags.AfmIntegrationEnabled = false`. This is a product decision (retire vs. reintroduce a second Google/Firebase auth flow vs. something else), not something to solve with more code without direction.
2. `PrivateOrderShares` identifier resolution — server issue #4.
3. A dashboard page to list/revoke `ApiToken`s — server issue #5.
4. `flipperOrders`, `playercount`, and `be/festivities` haven't been seen working live yet (just not exercised by gameplay during the test session) — worth a quick real-world confirmation next time the client runs, though there's no reason to expect them to behave differently from the three routes already confirmed. Not filed as its own issue — a verification task, not a bug or a feature.
5. ~~Redeploy the Portainer stack to ship the BunkerWeb URL-length fix~~ — done, confirmed fixed live (see "SSH access granted" above). If more direct server access is wanted going forward, set up a Portainer stack webhook for TrimsSilver-Server so Claude can trigger a redeploy itself without needing broader Portainer data-volume access.
6. Verify the Farming & Breeding Calculator's signed-in flows live: specs auto-fill from a real synced character's `AchievementSnapshot`, and Settings save/load round-trips — not exercised in the session that built the feature (no live Discord session in the automated browser).
7. Farming Calculator fast-follows, if wanted — server issue #8: Outlands farm territories (per-territory biome/quality bonuses instead of the fixed Royal-city table — deliberately cut, no small stable data source for it) and Beast/rare-mount breeding (deliberately cut, no clean AODP sell path).
8. Farming Calculator: split animals with more than one use (e.g. Goose meat vs. eggs) into separate comparable result rows instead of always picking the product cycle when one exists — server issue #9, noted by the user, not blocking.
9. Verify the Crafting Calculator live via a real browser session (Playwright MCP), the way Market Prices and Farming were — server issue #12.
10. Client issue #6 — silent auto-update for Linux/macOS, matching the Windows experience.
11. Crafting Calculator fast-follows, if wanted — server issue #10 (Resource Return Rate auto-calculation) and #11 (My EMV price mode for quality 1-5 equipment).
12. Verify the Flipper live via a real Discord session + real client-scanned market data (this session only verified it against seeded synthetic DB rows) — server issue #15, same pattern as #9/#12 above.
13. Flipper fast-follows, if wanted — server issue #13 (Public Flips, cross-user aggregation via `MarketOrder.contributeToPublic` — now partially superseded by the AODP-based Public Flips shipped the same day, see above; #13 is specifically about the private-data cross-user variant) and #14 ("consumed" tracking + hourly statistics chart), both deliberately cut from V1 scope, not blockers.
14. Fix client issue #7 — "Private Flips Mode" needs to persist and/or stop resetting on logout, so private Flipper data actually shows up without the user remembering to re-check a box every session.
15. Verify the Journals Calculator's signed-in Save/Load Settings round-trip live (same gap as Farming/Crafting/Flipper) — server issue #18.
16. Journals Calculator fast-follow, if wanted — server issue #17 (auto fill-cost for the 9 non-gathering families, if a crafting-fame-per-item/PvE-fame data source is ever found). EMV price mode (former issue #16) is done, see above.
17. Verify Craft Finder live via a real browser session (Playwright MCP) — server issue #19, same pattern as #12/#15/#18.
18. Craft Finder fast-follows, if wanted — server issue #20 (transport cost/time between buy/craft/sell cities not modeled; "simulation city" selector has no numeric effect today, no per-city fee table exists anywhere in this codebase). Tuning the default liquidity threshold curve against real usage isn't filed as an issue — no data to act on yet, revisit once the user has looked at a few real rankings.
